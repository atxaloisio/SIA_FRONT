import { PasswordEqualsDirective } from './password-equals.directive';

describe('PasswordEqualsDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordEqualsDirective();
    expect(directive).toBeTruthy();
  });
});
